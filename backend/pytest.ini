[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
# 동시성 문제 방지를 위해 순차 실행 (병렬 실행 비활성화)
# -x: 첫 번째 실패 시 중단
# -v: 상세 출력
# --tb=short: 짧은 트레이스백
# --strict-markers: 마커 검증
addopts = --tb=short -v --strict-markers

